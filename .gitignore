# Build outputs
/target
/.oxide-artifacts
*.ll
*.ptx
*.ppm

# Frame dumps (regenerated by the demo binaries; the GIFs assembled from them are kept)
/frames/
/fit_frames*/

# Deliberately NOT ignored:
#   Cargo.lock  — application workspace: reproducible builds stay
#   assets/     — test meshes the demos load
#   *.gif       — showcase media embedded in README.md
